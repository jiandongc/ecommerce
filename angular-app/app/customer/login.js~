var login = angular.module('customer', ['ngRoute']);

productDetail.config(['$routeProvider',
  function($routeProvider) {
    $routeProvider.
      when('/login', {
        templateUrl: 'customer/login.html',
      });
  }]);
